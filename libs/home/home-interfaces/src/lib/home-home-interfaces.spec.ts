import { homeHomeInterfaces } from './home-home-interfaces';

describe('homeHomeInterfaces', () => {
  it('should work', () => {
    expect(homeHomeInterfaces()).toEqual('home-home-interfaces');
  });
});
